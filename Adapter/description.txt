Allows 2 incompatible interfaces to work together

Used when the client expects a target interface

Adapter class allows the use of the available interface and the target interface

Any class can work together as ong as the adapter solves the issue that all classes must implement every method defined
by the shared interface