It allows you to modify an object dynamically

Used when you want the capabilities of inheritance with subclasses, but you need to add functionality at runtime

More flexible than inheritance

Simplifies code because you add functionality using many simple classes

Can extend new cause instead of rewriting old code